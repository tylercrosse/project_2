<% if @item %>
<%= image_tag(@item.image, class: "thisitem") %>
<% end %>
<% @items.each do |item| %>
  <% unless @item && item.id == @item.id %>
    <%= link_to image_tag(item.image), item_path(item) %>
    <% end %>
  <% end %>
